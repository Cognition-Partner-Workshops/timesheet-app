{
  "name": "timesheet-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright E2E tests for the timesheet app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
